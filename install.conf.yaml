- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/.nvm
    - ~/Code

- shell:
    - command: ./scripts/setup_homebrew.zsh
      stdout: true
      stderr: true
#  - [git submodule update --init --recursive, Installing submodules]
